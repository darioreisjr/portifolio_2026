export interface Project {
    img: string;
    title: string;
    description: string;
    repository: string;
    deploy: string;
    skill: string[];
    icon: string[]; // Caminhos para os ícones
    createdAt: string;
}